# 写作与维护风格

本仓库以 GitHub Markdown 作为主要阅读格式。章节笔记优先写成学习式讲义：读者应能沿着问题、概念、证据和工程判断逐步理解主题。维护时优先保证简洁、可读、可长期维护，不追求站点专用语法。

- 每个 Markdown 文件只保留一个 H1。
- 示例代码必须放在成对的 fenced code block 中；正文里的 `#` 只用于 Markdown 标题。

## 语言风格总则

- 默认面向第一次系统学习大语言模型的读者写作。不要假设读者已经看过课程视频、论文原文或前后章节；首次出现的关键概念、变量、实验设置和工程选择，应在本节内给出足够上下文。
- 章节和大节先回答“为什么要学这一段”和“这一段解决什么问题”，再进入概念、机制、代码、图表或公式。读者应能沿着问题一路读到判断，而不是先面对一堆名词。
- 写作语气采用教学式领读：可以使用“先看一个现象”“这个例子说明”“现在把直觉写成公式”这类承接句，但不要写成口语流水账或课堂转录。
- 复杂概念先给可感知的场景、例子或类比，再落到正式术语、变量关系和适用边界。类比只用于打开直觉，不能替代技术定义和证据。
- 优先使用短句和自然段推进。一个段落只承担一个主要教学动作，例如提出问题、解释机制、读图、读代码、比较方法或总结判断。
- 段落表达要直接：同一段里先定义首次出现的术语、字母、颜色、池名或变量，再写它们之间的关系和结论。不要把定义挪到下一段，也不要让读者先猜图例再回头找解释。
- 避免过度论文腔、资料堆砌和结论先行但缺少推导的写法。引用材料、榜单和技术报告时，正文要把它们吸收成学习路径，而不是把来源内容并排搬运。
- 每个重要判断都应让读者知道它来自哪里：可以来自公式推导、代码输出、图上趋势、实验设置或工程约束。不要只写“显然”“可以看到”“效果更好”。
- 讲义可以保留温和的学习节奏，例如阶段小结、常见误解和实践提醒；但这些内容要服务理解，不要变成固定模板。

## 段落与空行

- 独立段落之间必须保留一个空行。Markdown 源码里的单个物理换行只作为同一段落的源码换行，不承担换段含义。
- 块级元素前后保持清晰空行：标题、普通段落、图片、图注、alert、列表、display math、表格和 fenced code block 之间默认用空行分隔。
- 图注后若继续解释图片，图注和正文说明之间保留空行；连续图片可以逐张按“图片 -> 图注 -> 说明”组织。
- 不使用行尾两个空格制造 hard break。确实需要换段时写空行；确实属于同一段时合并为一个自然段。
- 空白行保持为空行，不保留空格或制表符；正文和代码示例行尾也不保留多余空白。

## 段落与承接

- 连续无空行的物理行超过 6–8 行（即单段 > 8 行）时，强制拆段或补承接句；长段常常是讲义读起来「闷」的主因。
- 一个段落只承担一个主要教学动作：提出问题、解释机制、读图、读代码、比较方法或收束判断，不要把多个动作塞进同一段。
- 紧邻长公式、长表格或长代码块的正文段需要至少一个承接句，把读者注意力从「符号账本」拉回「工程判断」。承接句可以承接上文的判断、点明本节的目的、或者预告下一段要回答的子问题。
- 拆段时优先按教学动作切：例如把「定义 + 公式 + 工程后果」拆成「定义段 → 公式段 → 后果段」，而不是按源码行数机械切。
- 单段承载动作超过 2 个（如「定义 + 公式 + 工程后果 + 引出下一段」）时拆段。
- 长条件句要拆短；如果一个句子里同时含「if / 否则 / 同时 / 但是 / 因此」三组以上转折，优先拆成两句或三句短句。

## 公式与排版

- 行内公式使用单美元符号包裹；独立公式使用双美元符号包裹，并让开始和结束分隔符各自单独成行。独立公式前后各保留一个空行，避免 GitHub 把双美元符号当成普通段落文本。
- 正文中不使用反斜杠形式的行内或块级公式分隔符，例如 `\(...\)` 或 `\[...\]`。
- 公式分隔符内侧不留首尾空格，行内公式不要包进反引号。
- 行内公式与普通正文、括号和标点之间默认都保留一个空格，例如 `变量 $B$ 表示 batch`、`空间（ $d_k = d_{\text{model}} / h$ ）`、`公式 $B$ 。`
- 冒号、括号、破折号后接行内公式时，也保留一个空格，例如“记为： $F_{\text{forward}}$ ”、“， $s$ 是 sequence length”、“是 $s$ 。”，避免公式和中文正文黏连。
- 较长的独立公式优先写成多行 display math，避免把整条公式塞进一行。
- 本仓库默认以 GitHub Web 的 MathJax 预览为兼容目标。写公式时优先使用 GitHub 原生支持的单美元行内公式和双美元 display math；只有在 Markdown 冲突明显时，才改用 fenced `math` code block。
- 多字母下标如果表示词语或配置名，优先写成 `\text{}` 或 `\mathrm{}`，例如 $d_{\text{model}}$ 、 $N_{\text{param}}$ 、 $D_{\mathrm{KL}}$ 。
- 代码属性和数学变量分开写：正文里用 `h2.grad` 表示 PyTorch 属性，公式里用 $G_{h_2}$ 这类数学记号，并在文字中说明二者对应关系。
- 避免将代码风格标识符直接塞进公式，例如“把 `w1.grad` 包进数学环境”或“在 `\text{}` 里硬塞 snake_case 名字”。正文里用反引号写代码名，公式里改用数学记号，并在文字中说明二者对应关系。
- GitHub Web 会禁用部分 LaTeX 宏，仓库公式里不要使用 `\operatorname{...}`；函数名优先写成 `\mathrm{...}`，或移到正文说明。
- 若必须在 `\text{}`、`\mathrm{}` 等文本宏里写字面下划线，必须写成 `\_`。
- 一句话里如果同时混有代码、多个行内公式和长中文说明，优先拆成两句；必要时把关键关系单独提成 display math，减少 GitHub 预览歧义。
- **禁止在数学环境里塞 snake_case 标识符**（`num_tokens`、`max_seq_len`、`input_ids` 等）。正文中需要时改用数学记号 + 文字定义：「按 $\sqrt{T_i}$ 归一化（$T_i$ 是样本 $i$ 的 token 数）」，而不是「按 $\sqrt{\text{num\_tokens\_per\_sample}}$ 归一化」。

## 公式账本

参数 / 显存 / 计算量 / 通信量 / KV cache / pipeline bubble / routing / loss 等公式账本是讲义的核心，应严格遵守以下硬约束：

- **符号首次出现即定义**：公式中的每个字母、上下标、希腊字母（$N_{\mathrm{param}}$、$B$、$s$、$d_{\mathrm{model}}$、$d_{\mathrm{ff}}$、$L$、$h$、$K_{\mathrm{kv}}$、$G$、$\rho$、$\hat{A}$、$\epsilon$ 等）在第一次出现时必须用文字给出定义，不允许读者回头猜。
- **公式与代码示例对齐**：讲义代码示例（`lecture_NN.py`）里出现的数字必须与正文公式推导一致；不一致则改代码或改公式，并在章节末「来源与更新记录」注明。
- **公式后立刻给承接句**：display math 之后下一段必须给出「这个公式意味着什么 / 工程后果是什么 / 与什么章节承接」的承接句，不允许公式孤立成段。
- **长算式拆多行 display math**：单条公式 $> 4$ 行时，用 `aligned` / `split` / 多行换行拆开，避免 GitHub Web 预览把整条公式塞进一行。
- **变量与符号的跨章节一致性**：同一符号在不同章节指同一变量。如 $N$ 在 ch8 = 总参数量，在 ch2 = token 数，必须分别定义或换字母。同一硬件数字（H100 BF16 dense 989.5 TFLOP/s）在所有章节口径一致。
- 避免将代码 span、强调和公式塞进同一个 Markdown 强调块；例如把桶宽度说明写成普通段落：**桶宽度** $\epsilon$ 与量化步长对应。
- 自然语言赋值优先写成等式，例如 $B_r = 128$ ；不要写成符号和中文动词黏在一起的半公式。
- 中文与英文、数字之间默认留空格；固定代码标识、文件名、URL、路径和链接目标不拆。
- 连续字段说明优先使用平铺列表，不把多个 `**字段**：...` 挤在同一行。
- 数学乘法、矩阵尺寸、FLOPs、复杂度和张量形状使用公式表达，例如 $M \times K$ 、 $2 \times B \times D \times K$ 。
- 硬件数量和自然语言倍数不用乘号写法，优先写成 `16 张 H100`、`约 100 倍`、`8 倍`。
- ASCII 示意图必须放在 fenced code block 中；边框、箭头和中文说明应尽量对齐，避免裸露在正文里。

## 语言与术语

### 学习式讲义

- 章节开头固定交代学习目标、问题主线和读完后能理解或判断什么。目标应贴近读者任务，例如“能估算训练显存”“能解释 KV cache 为什么占显存”，避免空泛写“了解相关知识”。
- 章节正文沿同一条教学路径推进：问题引入 -> 直观解释 -> 正式概念 -> 图、代码或公式 -> 工程判断 -> 小结。必要时可以拆成多小节，但不要把定义、公式、代码和结论割裂成互不相连的材料堆。
- 小节开头先提出本节要回答的训练、系统或概念问题，再进入细节。读者看到小节标题和第一段，就应知道接下来要解决什么困惑。
- 核心段落应回答四个检查项：读者为什么需要这一段，前置概念是什么，证据来自哪里，工程上怎样使用。
- 复杂系统先从可感知的数据流、计算流或资源瓶颈说起，再引入正式术语。例如先说明 tensor、token、rank、KV block 在系统里如何移动，再写 parallelism、scheduler 或 cache 的名字。
- 比较两种机制时，分别说明各自要解决的问题、成立条件、收益指标和留下的瓶颈。不要只写 A 比 B 好，也不要只罗列优缺点。
- 每个核心图下方紧跟解释：这张图回答什么问题、比较什么变量、支撑什么判断、使用时有哪些限制。
- 课程材料用于校准技术对象、讲解顺序和边界。正文只保留合成后的讲义表达；课程分工和来源记录放在章节末或 `sources/`。

### 代码、图表与公式的教学领读

- 代码块前先说明这段代码要验证什么现象、涉及哪些关键变量、读者应重点看哪几行。代码块后解释关键输出、异常现象和它们对应的概念。
- 可运行示例优先服务一个清晰问题。避免为了完整而放长代码；长代码若不可避免，应先给结构说明，再分段解释关键路径。
- 运行输出不是装饰。保留 stdout 时，正文要说明输出里的数字、形状、误差或速度差异意味着什么。
- 图表先说明读者要观察的变量，再解释坐标轴、曲线、表格字段和可见关系，最后落到机制、工程判断或边界条件。不要停在“图中展示了某结果”。
- 图解不要用“读法是”“看这类图时”“工程读法是”“这张图告诉我们”之类的元提示做句子骨架。直接写图中变量、曲线或颜色代表什么，再写它说明了什么。
- 多子图按左/中/右或上/下拆成独立段落，每段只负责一个子图或一个变量轴。先说每个子图的横纵轴和图例，再说它和其他子图的差异。
- 公式先给直觉，再定义变量，然后说明公式回答的问题和适用条件。不要让公式孤立成段，也不要把未解释的符号留给读者猜。
- 图、代码和公式若服务同一个判断，应写出它们之间的关系。例如“公式给出复杂度上界，代码输出展示实际显存峰值，二者差距来自 activation 保存策略”。

### 全局原则

- 课程常用英文术语不要硬翻或与中文黏连。首次出现时可以中文解释，例如 “tokenizer（分词器）” 或 “activation（激活）”；后续技术段落优先保留英文术语。
- 代码变量、API、stdout、文件名、论文题名和真实引用中的原始字符串保持原样，不为了统一术语而机械替换。
- 避免把英文缩写、课程术语和中文名词直接拼在一起；也避免把稳定英文术语反复翻成生硬中文。
- 课程 video 或 slides 里的 shorthand、比喻和口语评价不要直接搬进正文。先翻译成具体实验条件、变量关系和工程判断；例如把 `scaling ladder` 写成“一组逐级放大的小模型预实验”，把现场赞叹落到“趋势清晰、观测区间接近线性、收益随规模减弱”等可检查描述。
- 复杂系统策略不要压缩成一句口号。至少交代切分维度、通信模式、主要收益和留下的瓶颈，避免读者只记住缩写而不知道系统在搬什么数据。
- 正文优先使用正向讲义表达，直接陈述成立条件、变量关系、适用范围和限制。比较两种机制时，分别说明用途、边界和工程后果。
- 调整章节逻辑组织时，同时看对应 slides 和 video 讲解：slides 决定技术对象、公式和配图，video 讲解帮助确认概念出现顺序、铺垫方式和边界条件。正文要把这些材料合成一条可线性阅读的讲义路径。
- 新增或修订课程材料时，先复核 slides/video 的讲解意图，再决定正文如何组织。正文优先吸收成问题主线、机制、例子和工程判断，公开来源写课程 slides/video，不写本地整理路径或内部整理过程。
- 章节应能线性阅读。不能假设读者已经看过课件或前后章节；首次出现的核心术语、变量账本和训练决策，应在本节内给出足够上下文。
- 图解段落应把口头强调转换成技术判断。口语里的惊讶、赞叹或现场推进，要落到 `slope`、`offset`、`scale-invariant`、`sample efficiency`、`active compute` 等可检查关系上。
- 避免用内部整理动作或现场推进词当句子骨架，例如“继续推进到某图”“补充某个细节”“这张图给出做法”“本段判断是”。正文直接写变量关系、图上证据和工程后果。
- 避免以反设开头、再转向新读法的句式。
- 避免把“不是……而是……”“是……而不是……”“不是……而非……”“不是简单的 X，而是 Y”这类否定转折句当成常用骨架。需要纠偏时，先正面写清正确读法；需要比较时，直接分别说明两种机制的用途、边界和工程后果。
- 避免以“本章 + 否定动词 + 当成”开头的章首模板。需要比较时，直接分别说明两种机制的成立条件、差异和工程后果。
- 核心图的解释应紧跟图注，避免连续多张图后才集中解释。多图对照可以有统一总结，但每张核心图下至少交代它回答的问题、变量账本或机制差异；只有同一机制的无正文小图标可以例外。
- 图解采用领读式写法：先说这张图回答的工程或概念问题，再说明图中变量、曲线或表格项，接着指出可见关系，最后落到机制、决策或边界。说明坐标轴后必须立刻服务一个判断，不能停在看坐标本身。
- 核心图段落按“训练决策 -> 图上证据 -> 可迁移结论 -> 边界条件”拆开；长条件句要拆短，避免把证据、结论和 caveat 挤在一句里。
- 对密集图表，优先使用 2-4 个短段：先定义图中反复出现的标签、字母、池子或坐标轴，再分别读左/中/右子图，最后用一段收束成训练结论。不要把定义、图上关系和工程后果塞进同一句。
- 避免把图解写成泛化口令，也避免只要求读者关注坐标轴、斜率或曲线整体位置。需要谈 slope、offset、最低点或宽区域时，直接绑定到具体图和工程含义，例如“近似平行说明这个数据配比主要改变 offset，外推风险比 slope 改变小”。
- 课程课件、公开课程视频和相关材料用于校准顺序、术语和技术口径；正文应写成自洽讲义，不写“视频中说”“课件里说”“Lecture X 说”这类元叙述。课程来源统一保留在章节末来源记录或 `sources/` 中，图注和表注不写来源、查阅日期。
- 不在章首设置 `## 与 CS336 的关系` 这类课程关系小节。章节开头服务学习目标、问题主线和工程判断；课程映射、lecture 分工和材料边界写入章节末来源记录或 `sources/cs336-2026.md`。

### 跨章引用格式

跨章引用统一使用「第 N 章 §X.Y 章节标题」格式，不允许缩写为 `chapterN` / `第 N 章 §X.Y`（省略标题）/ `第 N 章`（省略 §X.Y 与标题）。章节标题必须与目标章节实际 H2/H3 一致，跨章节 audit 跑 `rg` 时按此 pattern 抓不一致：

```bash
rg -n "第 [0-9]+ 章 ?(§[0-9.]+ ?)?[一-鿿]" docs/
rg -n "chapter[0[0-9]]+|chapter ?one|chapter ?two" docs/
```

第二种 rg 抓 `chapter3` / `chapter one` 类缩写，第一种 rg 抓「第 N 章」与可能的 §X.Y 后是否接章节标题。缩写形式（`ch3` / `ch11 §11.5.5`）仅在 sub-agent 内部 prompt 与脚注使用，正文不允许。

## 修辞层规则

讲义正文按权威源陈述事实，不暴露作者的章节组织选择、写作计划或免责意图。本节专注**作者声音穿帮**（与「验证流程不进正文」节审计元句并列、互不重复）：

- 「这里使用 / 本文采用 / 本文以 … 为样例 / 本文以 X 为例」——作者的章节组织选择，读者不需要知道作者选了什么。
- 「后续章节会 / 后文将 / 后文会明确 / 后面单独讨论 / 下一节会展开 / 下一节转到」——作者把写作计划提前告诉读者，等于让读者读一遍章节大纲。
- 「不等于所有 / 不是说 / 并非主张 / 并不意味着 / 但愿不要误读 / 读者不要把本文当作」——免责声明；按权威源给数字即可，不需要预先撇清自己。
- 「按 CS336 Lecture N 的口径 / Lecture X 说 / 课件中讲 / 字幕原话 / 视频中说 / 讲者已在 / 属讲师口误」——课程来源元叙述；正文应直接陈述事实，课程来源统一写到章节末。

**审计 / 搜证元句**（如「本节以 X 为准 / 未经 X 验证 / 缺口 / 未能核 / 待 Y 披露 / 建议引用时直接查证」）归「验证流程不进正文」节管，本节不重复列举。

正确做法：

- 直接陈述事实与数字（数字 + 出处写在章节末「来源与更新记录」或来源映射文件）。
- 比较两种机制时，分别说明各自的用途、边界和工程后果，不替读者做选择；需要承诺差异时直接写「§5.8 单列」，而不是承诺「后文会」。
- 课程来源统一保留在 `sources/cs336-2026.md` 或章节末来源记录，正文不写「Lecture X 讲」式承接。
- 当一段落不可避免需要「承诺后文会如何」，把承诺句改成事实陈述：写「第 N 章 5.8 单列 B200 / Blackwell 的差异」，而不是「后文会单独讨论」。

上述句式比 `rg` 元叙述关键词更细一层，建议 audit agent schema 增加 `author_voice_break` 字段，专门抓这一类作者声音穿帮，并配合 `rg -n` 在 commit 前扫一遍。

### 术语表

- 计算总量写 `FLOPs`，硬件吞吐率写 `FLOP/s` 或 `TFLOP/s`；代码变量、课程 API 和运行输出里的 `flops` 等标识符保持原样。
- 固定训练计算预算下的实验方法写 `IsoFLOP 分析`，对应曲线写 `IsoFLOP 曲线`，固定预算写 `FLOP budget`。
- Scaling 语境写 `scaling law`、`power law`、`log-log plot`、`data scaling`、`model scaling`、`compute scaling`、`critical batch size`。
- Chinchilla 相关语境写 `Chinchilla`、`train-optimal`、`inference-optimal`、`overtraining`、`tokens per parameter`、`muP`、`WSD`。`muP` 首次可说明常写作 $\mu\mathrm{P}$ ；不要在同一章混用 Unicode 希腊字母变体或 `m` 前缀变体。
- 英文资料中的 `recipe` 表示一组做法；正文默认拆成具体对象来写，例如训练条件、训练设置、关键训练选择、数据处理方案、数据混合方案或 `scaling workflow`。
- 涉及从小实验走向大训练时，可以写“放大流程”，并说明模型规模、数据量、compute、width、batch 等变量如何变化。
- 避免把 `recipe` 和泛称词直接拼接；相关泛称词包括“细节、证据、分析、模型、同等、纳入、扩展”。需要说明 tokenizer、数据处理、optimizer、LR、scheduler、训练长度等条件如何保持一致、沿用、复核或按同一规则调整。
- 推理缓存写 `KV cache`。首次解释时可以括注中文含义，后文不混用大小写、连字符或无空格变体。
- 推理阶段写 `prefill`、`generation`；若使用 `decode`，需说明它指自回归 generation 阶段或解码过程。推理指标写 `TTFT`、`latency`、`throughput`。
- 推理优化至少说明优化对象是 weight memory、KV cache、scheduler、kernel，还是模型分布；不要只写“加速推理”而不说明瓶颈来源。
- 推理章节的小节收束应并入自然段落，直接交代瓶颈对象、收益指标和质量风险，避免反复用固定标签收尾。
- 推理结构术语写 `GQA`、`MQA`、`MLA`、`CLA`、`sliding-window attention`、`draft model`、`target model`、`speculative sampling`、`speculative decoding`。
- 推理 serving 术语写 `continuous batching`、`iteration-level scheduling`、`selective batching`、`PagedAttention`、`KV block`、`logical block`、`physical block`、`prefix caching`、`copy-on-write`、`RadixAttention`。
- 分布式 collective 在正文中写 `all-reduce`、`reduce-scatter`、`all-gather`、`all-to-all`；PyTorch API、代码和 stdout 保留 `all_reduce`、`reduce_scatter_tensor` 等 snake_case。
- 分布式训练正文保留 `collective`、`rank`、`world_size`、`process group`、`global batch`、`local batch shard`、`micro-batch`、`activation`、`pipeline bubble`、`bubble time`、`bubble fraction`、`zero-bubble pipeline` 等课程常用术语。
- 并行策略术语首次可写 `sequence parallelism（序列并行，SP）`、`context parallelism（上下文并行，CP）`、`expert parallelism（专家并行，EP）`；后续技术段落优先使用英文术语或缩写，不在“序列并行 / sequence parallelism”之间来回切换。
- 分布式并行策略收束时，直接说明切分维度、通信模式、显存/吞吐收益和剩余瓶颈；避免用外部材料、修订过程或纠偏标签作为正文支架。
- `bubble` 在 pipeline parallelism 语境中保留英文；需要中文解释时写“空闲窗口”或“空闲开销”。
- CUDA/Triton/NCCL 语境写 `CUDA kernel`、`global memory`、`shared memory`、`thread block`、`warp`、`occupancy`。`CTA（Cooperative Thread Array）` 可作为 `thread block` 在 PTX / profiler 语境里的补充称呼。
- GPU/performance 术语写 `SM`、`HBM`、`Tensor Core`、`arithmetic intensity`、`memory-bound`、`compute-bound`、`kernel fusion`、`recomputation`、`memory coalescing`、`tiling`、`FlashAttention`、`online softmax`、`Triton program`、`PTX`。首次解释时可括注中文含义，后续不反复硬翻。
- `Tensor Core` 需要看上下文：NVIDIA GPU 语境通常指 SM 内部的矩阵乘法专用电路；TPU 资料中的 tensor core 可能指包含 MXU、Vector Unit 和本地内存的处理器级单元。
- Transformer 架构术语写 `Pre-norm`、`Post-norm`、`RMSNorm`、`SwiGLU`、`GeGLU`、`RoPE`、`QK norm`、`soft-capping`；若需要中文解释，可首次括注，后文保留课程常用英文写法。
- MoE 相关术语写 `top-k`、`token-choice routing`、`expert-choice routing`、`router logits`、`z-loss`、`per-expert bias`、`per-device balancing`。
- MoE 系统路径写 `router`、`dispatch`、`all-to-all dispatch`、`expert compute`、`combine`、`all-to-all combine`。
- MoE 并行和实现语境写 `expert parallelism`、`expert tensor parallelism`、`expert data parallelism`、`expert parallel group`、`MegaBlocks`、`dropless`；避免将 `expert` 反复硬译成“语义专家”或“领域专家”。
- 技术语境中使用 `prompt`；首次面向读者解释时可写“提示词（prompt）”，后续不在普通正文里混用大写变体。

### 基准与模型的 casing 一致性

- 基准测试、模型系列、模型变体的官方拼写在同一份文档内必须统一（连字符、空格、大小写都算拼写的一部分）。`CyBench`、`Cybench`、`CYBENCH`、`cy-bench` 在同一仓库里只允许出现一种。
- 涉及基准名时先 WebFetch 官方页面（arXiv abstract / 模型卡 / 官方 README / 原始公告）拿一次官方拼写，再决定章节用哪一种。常见落点：arXiv 标题字段（title case）、官方 README 第一次出现的拼写、HuggingFace model card 顶部 README 引用块。
- 修改一处官方拼写后，必须在同一 commit 内对全仓库 `rg "<old>" docs/ sources/`，把残留旧拼写扫一遍。casing 归一化常被遗漏在标题、图注、表格、本章总结与参考文献等多处，不能只改正文就 commit。
- 一致性是文档级而不是章节级：同一份 `docs/chapterN/...md` 里同一基准出现 5 次时，5 次必须同形；如果某节需要明确区分「官方题面」与「口语叫法」，用「也称 / 又叫」括注一次即可，不要在后续几节换用别称。

## 图片

- 使用本地相对路径，优先放在同章节的 `images/` 目录。
- 图片编号采用二级节相关格式：`图 {章号}.{二级节号}-{节内序号}`。例如第 8 章第 2 节第 1 张图写作 `图 8.2-1`，第 8 章第 6 节第 12 张图写作 `图 8.6-12`。
- 使用 Markdown 图片语法，形如 `![图 8.2-1 简短说明]` 接相对路径 `(images/8-2-1-lowercase-english-slug.png)`。
- 图片下方保留斜体图注：`*图 8.2-1 简短说明*`。图注只写图题或必要讲义说明，不追加 `来源：...`、`查阅日期：...`。
- 图片文件名前缀与图号一致，把图号里的点号换成连字符：`8-2-1-lowercase-english-slug.png`。slug 使用小写英文和连字符，避免中文、空格、下划线、大小写混排或生硬翻译短语。
- 新增图片追加到所在二级节末尾，保持节内序号连续；避免使用字母后缀补丁式编号。
- Markdown alt text、图注和正文交叉引用必须使用同一个图号。
- 从 CS336 2026 课件、课程视频、论文、技术报告、官方文档或本地说明整理来的图片，公开来源统一写到章节末来源记录或 `sources/`。图注保持短，不在每张图下反复写来源。
- 模型、硬件、benchmark 等快速变化信息仍按“来源与日期”规则记录查阅日期；日期放在章节末来源记录、专题记录或来源映射文件中。
- Markdown alt text 和图注可以使用中文教学说明，但涉及课程术语时优先保留英文术语，例如 `collective`、`all-gather`、`activation memory`、`pipeline bubble`。
- 课程来源图优先使用原始英文课件图或原始截图；不要保留由机器翻译截图生成的教学图片。若现有图片只是文件名中文但图片内容是原始英文图，可以只重命名并补图注。
- 课程截图里的英文表格若影响首次阅读，可以保留原图，并在正文追加短 Markdown 表格解释字段和训练含义；不生成翻译版截图。
- 核心教学图不能只有图题。连续课程图后应补自然说明，交代图在比较什么、变量如何变化、能支撑什么工程判断；限制条件应融入正文，不必机械写成“不能推出什么”。
- 现有图片若不能支撑正文中的关键判断，优先从课程课件补充或替换；若图片只是文字总结页或重复信息，正文吸收结论即可。
- 避免使用 HTML image、div、p 等标签包裹图片。
- 不保留外部图片；新增或补充图片时先下载到本地，再使用相对路径引用。

### 图意核对（核心图写作硬约束）

核心图（被正文反复引用、承担工程判断的图片）在写作与审计时必须实际打开本地 PNG 核对图意，不能只看 alt text / 文件名 / 文件路径。工具：`Read images/...png`。

- **图意核对四个检查项**：(1) 子图标题 / 坐标轴 / 图例是否被正文正确转述；(2) 正文数字（与图上标注差距 >0.5 倍的）是否一致；(3) 图说的「机制解释」是否能在图中找到对应视觉证据；(4) alt text / 图注 / 正文交叉引用是否使用同一个图号。
- **常见图文不符 6 类**：(1) 图被替换（md5 与旧图不同，如 ch10 CCNet 工作图实际是 raw/target 示意图）；(2) 内容不符（图说的子图实际不存在，如 ch3「右图 BERT」实际只有右下子图是 BERT）；(3) 子图被笼统归类；(4) 图说覆盖不到图实际内容（图说只提中间一栏，实际三栏，如 ch2）；(5) 图说超过图实际内容（写了图上没有的子图 / 坐标轴）；(6) 图与代码示例架构不一致（图示三层 linear+ReLU，推导按两层纯线性，如 ch2）。
- **删图判定**：两图承载同一份视觉信息（即使 SHA256 不同，如课件截图 vs 论文原图）即视为重复；保留论文原图、删课件截图（lecture 装饰元素属元叙述）。
- **删图后必须按连号规则重排同节其他图号**，并跑 `rg 图 N-x` 全仓库验证没有悬空引用。

### 实证数字硬约束（tokenization / 词表 / 配置）

写「X 字符串切成 N 个 token id」、「模型配置 Y 含 Z 个 expert」等实证段时，必须**用脚本实际跑一遍**而不是凭印象：

- **tokenization 示例**：`tiktoken` / `sentencepiece` / `transformers` 类的 token 切分必须实际运行 tokenizer，把真实 id 序列与每个 id 对应的字符串写进正文 / 图说。印象记忆几乎一定写错：例如「年份 1885 作为 4 位数字整体成为 id 13096」实际 tiktoken 输出是 `[93447 Stan, 9201 ford, 673 ` was`, 24303 ` founded`, 306 ` in`, 220 ` `, 13096 `188`, 20 `5`, 13 `.`]`，1885 是 3+1 位两段而非 4 位整体。
- **模型配置**：`num_local_experts` / `n_routed_experts` / `swiglu_limit` / `num_hash_layers` 等配置字段必须 WebFetch HuggingFace `config.json` 或官方技术报告，按官方字面值写，不要凭印象拼凑。
- **数据集规模 / 词表大小**：以「约」「左右」「≈」等模糊词结尾的数字必须查 `dataset_info` / `vocab_size` 一手值。

写实证段时同步跑一个 `python3 -c "import ..."` 或 Jupyter notebook，把命令与输出附在章节末「来源与更新记录」或附录，便于下次审稿 / 复核时重新对照。

## 文件命名

- 稳定章节放在 `docs/chapterN/` 下，主笔记文件命名为 `chapterN_标题.md`。
- 章节配套代码、Notebook 和可运行示例放在同章的 `examples/` 目录，文件名使用
  小写 snake_case，例如 `topk_tc_moe.py`。
- 前言等跨章节内容放在 `docs/` 根部，使用数字前缀保持阅读顺序，例如
  `00_前言.md`。
- 避免在文件名中重复写“第 N 章”，章节序号由目录名和 `chapterN_` 前缀表达。
- 过时的导出版 PDF、站点构建文件和未被引用的旧资源不进入笔记仓库。

## 提示块

使用 GitHub Flavored Markdown Alerts；MkDocs 专用的 `!!! note` 不进入正文。GitHub 目前支持 5 种 alert，每种都有明确的语义边界：

```markdown
> [!NOTE]
> 这里写补充说明。

> [!TIP]
> 这里写实践建议。

> [!IMPORTANT]
> 这里写不可绕过的关键约束。

> [!WARNING]
> 这里写容易误解或需要谨慎使用的内容。

> [!CAUTION]
> 这里写继续操作会带来负面后果的内容。
```

普通引用仍然使用 `>`，只有明确属于说明、建议、警告的内容才改成 alert。五种 alert 的语义边界如下：

- `[!NOTE]`：补充背景、前置知识、旁路解释，或不影响主线但有助理解的细节。读者扫读时可以略过，不丢核心判断。**不应用来装作者选择或章节组织承诺**（见「修辞层规则」）。
- `[!TIP]`：实践建议、工程判断、调参经验，读者可以直接迁移到自己的训练栈或推理栈。
- `[!IMPORTANT]`：不可绕过的约束——必须满足才能让后续推理成立（如「这个公式假设 batch size 已减去 activation checkpointing 的等效开销」、「SageAttention 要求 head dim ≥ 64」）。
- `[!WARNING]`：常见误解、容易踩坑的边界条件、来源不稳定、实验结论不能外推的情况。读者读到要停下核对前提。
- `[!CAUTION]`：继续操作会带来负面后果的内容（如「在没有 warmup 的情况下直接把这个学习率套到 AdamW 上，训练会在前 200 步发散」、「不要把 speculative decoding 的 draft model 单独上线」）。

补充规则：

- 不要为了形式统一给每节都添加固定小结或提示块；只有它们能减少误解、连接上下文或沉淀工程判断时才使用。
- 每个大节可以用 1-3 句话自然收束，说明读到这里应理解什么、能做什么判断、下一节为什么需要继续推进；这种「学习检查」性质的收束优先写成普通段落，不是 alert。
- 学习检查句可以适度出现，例如「到这里，读者应能区分 prefill 和 generation 的显存瓶颈」。这类句子要具体，不写空泛的「本节介绍了很多内容」。
- IMPORTANT / WARNING / CAUTION 三档严格递进：先想清楚到底是「不满足就不成立」（IMPORTANT）、「不满足就误解」（WARNING），还是「继续做会有损失」（CAUTION），再选择 alert 类型；模糊时优先 WARNING，避免把 CAUTION 当 WARNING 用导致信息降级。
- NOTE 不应承载任何作者声明、章节组织承诺或免责声明——这些全部用普通段落或下沉到章节末「来源与更新记录」。

## HTML

- 正文中尽量不使用 HTML。
- 能用 Markdown 表达的内容，优先用 Markdown。
- 教学示例中的 HTML 可以保留，但应放在 fenced code block 中。
- Markdown 表格中不使用 HTML 换行标签。
- 单元格内容较长或需要多行说明时，优先改成分节列表。
- 只有每个单元格都能保持简短时，才使用表格。

## 来源与日期

模型、硬件、基准、榜单、厂商声明等变化快的信息，记录时应包含：

- 来源链接：优先官方文档、论文、技术报告或模型卡。
- 查阅日期：使用 `YYYY-MM-DD`。
- 状态：例如 `官方`、`论文`、`课程材料`、`社区观察` 或 `待复核`。

CS336 2026 的公开课件和课程视频可以作为课程材料来源。若某个解释来自视频口头讲解，
可在章节末来源记录中写 `CS336 2026 Lecture X video`，必要时补充公开视频时间点；
不要写不可公开复核的整理路径或文件名。

快速变化内容不单独放入另设目录。若必须记录，放在最相关章节或 `docs/topics/` 的来源与更新记录中，明确来源、查阅日期、状态和适用边界；不要写成永久排名或稳定常数。

### 章节末「来源与更新记录」约定

每章末的「来源与更新记录」按以下顺序组织，方便读者和 audit agent 同时查证：

1. **官方来源**：arXiv abs / 论文 PDF、HF model card、官方仓库 README、技术报告 PDF。列出查阅日期 `YYYY-MM-DD` 与状态（`官方` / `论文` / `社区观察`）。
2. **课程来源**：CS336 lecture 编号与映射，按 `sources/cs336-2026.md` 给出的对应关系引用，不写本地整理路径或字幕文件路径。
3. **本节事实声明的来源指向**：正文里出现「按 …」「参见 …」的具体定位（论文 § 编号 + 段首句、模型卡具体字段名等）。
4. **不确定项**：暂未核到一手源的条目按 `tentative` 标记处理，列在「不确定项」子段；不在正文写「待 X 披露后核对」「未能核」「缺口」等免责语，全部下沉到本节。

正文不写「以 X 为准 / 未经 X 验证 / 待 Y 披露」类免责语——这些全部下沉到本节，由 audit agent 用 `rg` 自动扫，并在 commit 前清零。

当某个章节涉及多个官方源的最新口径时，在「来源与更新记录」单独列一条「来源对齐」小节，记录哪些字段以哪个官方源为准、哪些字段多源对齐、哪些字段存在版本分歧。这样读者在引用具体数字时不用回头翻章节正文。

## 验证流程不进正文

讲义正文（章节主体与图注、表注）不写审计 / 搜证过程本身。本节专注**审计 / 搜证过程特有的元句**（与「修辞层规则」作者声音穿帮并列、互不重复）：

- `abstract 仅给出…` / `正文表述以论文原表为准` / `需按目标章节复核` —— 暴露论文阅读路径
- `建议引用时直接查证 X` / `按权威源为准` —— 把搜证动作推给读者
- `未经 X 验证` / `待 Y 披露后核对` —— 把「我还没核」告诉读者
- `以 Y 为准 / 未经 X 验证` / `本节以 X 为准 / 属讲师口误` —— 把作者认证意图搬上正文
- `未能核` / `缺口` —— 显式标记审计未完成

这些句式只在 `AGENTS.md` 维护审计流程用，**禁止**出现在 `docs/` 正文里，包括章节末「来源与更新记录」以外的来源记录小段。讲义正文要么按权威源直接给出事实（数字 + 出处），要么省略该数据点；不确定项按「章节末来源与更新记录」第 4 段「不确定项」子段处理，全部下沉到章节末，不在正文出现。

引用 arXiv 时，正文应基于论文正文 Figure / Table 与公式，而不仅是 abstract；abstract 仅用于核对标题、作者与提交日期（与 `AGENTS.md`「必须读原论文，不能只读 abstract」同步）。

「修辞层规则」节专管作者声音穿帮（作者选择 / 免责声明 / 写作计划承诺），「验证流程不进正文」节专管审计 / 搜证元句；两者分工明确，rg 自检覆盖两条规则的全部反例。
